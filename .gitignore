cenode_modules

# Next.js build output
.next

# Local uploads (use Vercel Blob or similar in production)
apps/main/public/uploads

# Vercel build logs (fetched locally)
last-vercel-build.log

# Environment variables (secrets, DB URL, API keys)
.env
.env.*
!.env.example
.env.local
.env.production
.env.development
node_modules
.vercel
